function display(balance) {
    const balance = getInput(0);
    return balance
}

const num = getInput(1);

function savingsBox(balance, num) {
    const savingsBox = balance + num;
    let sum = savingsBox;
    if (sum)   
    return sum;
}


function spendBox(balance, num) {
    const spendBox = balance - num;
    let difference = spendBox
     if (difference) 
    return difference;
}

module.exports = calculator-backend.js